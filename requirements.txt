# Requirements for doc-audit skill
# Install with: pip install -r requirements.txt

# Core dependencies (required)
python-docx>=1.2.0
lxml>=6.0.0
defusedxml>=0.7.1
jinja2>=3.1.0
openpyxl>=3.1.0
google-genai>=1.56.0
openai>=2.14.0

# LLM dependencies (optional - at least one required for audit execution)
# Uncomment the one(s) you want to use:
# google-genai>=0.2.0
# openai>=1.0.0
